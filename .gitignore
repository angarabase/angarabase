# Keep build artifacts out of git history.

*.tar.gz
*.zip
*.deb
*.rpm
*.dmg
*.exe
*.msi

*.sha256
sha256sums.txt

dist/
build/
out/

# Export metadata — generated by export.sh, not useful to external readers.
EXPORT_MANIFEST.json

